# DistanceMatrixScript
This script calculates the geospatial distance between two data frames containing Longitude and Lattitude. These data frames can contain more columns than just those, but will just require the `$` reference in the main for loop.
